# Arranque rapido.

```
sudo apt update && \
sudo apt install git xclip build-essential cmake python3-dev vim-gtk3 -y && \
git clone https://github.com/Tx0mas/vimrc && \
cp vimrc/.vimrc ~/ && \
rm -rf vimrc
```
